<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="licancan.com.comicdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoadActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".StartingActivity"
            android:launchMode="singleTask" />
        <activity
            android:theme="@style/notitle"
            android:name=".LoginSuccActivity"
            android:launchMode="singleTask" />
        <activity android:theme="@style/notitle" android:name=".GridDetailActivity" />
        <activity android:theme="@style/notitle" android:name=".XlistDetailActivity" />
        <activity android:theme="@style/notitle" android:name=".ImgActivity" />
    </application>

</manifest>